import { StyledBackground } from "../Styled-components/Styled";

const Background = (props) => {
    console.log("props", props.num);
    return (
        <StyledBackground />
    )
};

export default Background;